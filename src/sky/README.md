Ciels photo (Imagine) — base du `theme_frame`.

- `starfield-a/b/c.jpg` : même champ, trois instants de scintillement.
- `starfield-sparse.jpg` : réserve plus vide.
- `spikes.jpg` : piques de diffraction (référence de forme).

Les étoiles code (croix, losange) s’ajoutent par-dessus, rares.
Les GIF animés ne passent pas dans un thème Firefox : l’APNG assemble a→b→c en boucle lente.
